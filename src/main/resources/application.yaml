spring:
  application:
    name: dicom-proxy
  threads:
    virtual:
      enabled: true
  ssl:
    bundle:
      pem:
        dicom-bundle:
          truststore:
            certificate: "${CERTIFICATE_PATH}"

base-url : "${BASE_URL}"